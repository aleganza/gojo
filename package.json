{
  "private": true,
  "name": "gojo-monorepo",
  "scripts": {
    "dev": "concurrently \"pnpm --filter app start:web\" \"pnpm --filter electron dev\"",
    "build:web": "pnpm --filter gojo build:web",
    "build:desktop": "pnpm --filter electron build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}