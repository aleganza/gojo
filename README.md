commands tested on ubuntu only

from the root:
```bash
pnpm electron:prepare   # build expo web, copy build inside electron directory
pnpm electron:build     # arrange electron build, package electron build
```